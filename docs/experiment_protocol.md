# Experiment Protocol

1. Select a configured route.
2. Start run via CLI.
3. Execute normal forward command sequence.
4. Stop run via CLI.
5. Rebuild stats after batch runs.
6. Review accepted/rejected tables and EDA outputs.

Rejected runs are retained for traceability and QC auditing.
