# Baseline Method

We use route-level abstract steps and fit time-only behavior:

`T_total = T_start_offset + n * T_abstract_step + T_stop_offset`

Accepted run history updates per-route mean/std timing and step-time descriptors. No session/day recalibration is used.
